apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: website
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      name: postgres
  clusterIP: None   # IMPORTANT for StatefulSet